#!/bin/sh

set -- $(dimensions)
w=$(echo "$1 - 2" | bc)
x=$(( $1 / 2 - $w / 2 ))
y=$(( $2 / 5 ))
h=$(( $2 / 50 ))
font="tewi:size=9:antialias=true:autohint=true"

clipmenu -p "Clip:" -i -f -fn "$font" -wi "$w"
